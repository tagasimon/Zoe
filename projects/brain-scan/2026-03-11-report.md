# Weekly Workflow Discovery Report — 2026-03-11

**Sessions scanned:** 6
**Period:** Last 7 days
**Projects:** Zoe, Zoe (marketing-team context)

---

## Patterns Found

| Pattern | Sessions seen | Output type | Friction? | External system? |
|---------|--------------|-------------|-----------|-----------------|
| Email triage / inbox check | 2 | Prioritised list + tasks | Yes — manual gws setup every time | Gmail (gws) |
| Website analysis → convert to presentation | 2 (multiple runs) | PPTX / PDF | Yes — two separate steps, no pipeline | Web + PPTX skill |
| Asking "what skills do I need" | 3 | Skill recommendations | Low | None |
| StatusCash dev setup guidance | 1 (long) | Step-by-step instructions | High — Supabase, env, migrations, errors | Supabase, Next.js |
| Google Workspace CLI auth issues | 2 | Nothing — just fixing auth | High — SSL errors, verification blocks | gws CLI |
| Folder/file cleanup and reorganisation | 1 | Organised file structure | Low | None |
| Competitor research on client websites | 2 | Market report / audit | Low | Web search |

---

## Recommendations

### Build as Skills

- **`email-triage`** — Already in backlog. Priority 1. Appears in 2 sessions this week alone. Read Gmail via gws, sort by urgency (proposals > personal > newsletters), surface what needs a reply, create tasks. Estimated frequency: daily.

- **`dev-session-handoff`** — Each StatusCash session requires re-explaining what was built. Skill reads the codebase state (routes, migrations, TODOs) and outputs a "here's where we are, here's what's next" briefing. Eliminates re-explaining. Estimated frequency: every StatusCash session.

- **`website-to-pitch`** — Simon repeatedly runs a marketing audit or CRO analysis on a URL, then immediately asks to turn it into a presentation. This two-step workflow should be a single skill: input a URL, output a PPTX pitch/report. Estimated frequency: every client prospecting or proposal session.

### Add to CLAUDE.md

- Simon frequently asks "what skills do I need" — add a note to proactively surface skill suggestions when a repeated workflow is detected during a session, rather than waiting for him to ask.

### Investigate as MCP Plugin

- **Google Workspace MCP** — Simon had multiple sessions with gws CLI auth friction (SSL errors, Google verification blocks, re-auth). An MCP server for Gmail + Tasks + Drive would eliminate all CLI issues and make email/calendar/tasks seamless. This would also unblock `email-triage`.

### Consider as Agent

- **`proposal-agent`** — Goes further than `proposal-builder` skill: research the company online, pull brand assets, generate a full proposal, export as PDF, and create a follow-up task in Google Tasks — all in one run. Autonomous multi-step.

### Skip (one-off or too specific)

- Claude permissions popup question — one-time setup, not a workflow
- Folder cleanup/reorganisation — happened once during initial setup, unlikely to recur often

---

## Suggested Next Actions

1. **Build `email-triage`** — Highest frequency this week. Requires gws to be working (it is). Would save time daily.
2. **Build `dev-session-handoff`** — StatusCash is now the main active build. Every session loses time re-establishing context.
3. **Investigate Google Workspace MCP** — Would eliminate recurring auth friction and power email-triage + future calendar features.
